#!/bin/bash

# Set GITHUB_USER from optional local file (create ~/.config/github_user with your username)
# or set it in ~/.bash_additions / ~/.zshenv.local
if [ -f "$HOME/.config/github_user" ]; then
	export GITHUB_USER=$(cat "$HOME/.config/github_user")
fi
